---
doc-version: 1.11.0
doc-status: active
doc-updated: 2026-09-15
---
# 未完成事项

> 文档状态：Active
> 最近复核：2026-09-15
> 负责人：产品闭环与发布治理

## 目的

本文件仅记录未完成项。已完成实现历史请放在 `docs/archive`；可执行的测试用例请放在 `docs/testing`。

## 当前基线

请同时参考这些文档：

- `docs/baseline/PROJECT_BASELINE.md`
- `docs/guides/staged-development-plan.md`
- `docs/guides/runtime-instance-and-regression-closure-plan.md`
- `docs/reference/runtime-closure-design-implementation-review.md`
- `docs/testing/runtime-publication-acceptance-cases.md`
- `docs/guides/runtime-security-and-call-audit.md`
- `docs/testing/runtime-security-audit-cases.md`

## 当前调度与专项入口：2026-09-14

旧任务包 A/B/C 已被后续治理实现和 OBS/SEC 专项分解覆盖，不再作为三个待从零开发的任务包。逐包状态以[可观测性完成复核](../guides/runtime-observability-completion-review.md)及[安全执行台账](../guides/security-development-execution-status.md)为准；前一波依赖见[活跃任务推进记录](../audits/2026-09-14-active-task-dependencies.md)，当前增量见[单跳凭据、容量样本与诊断UI](../audits/2026-09-14-single-hop-capacity-diagnostics-wave.md)。

| 工作 | 当前边界 | 依赖与下一步 |
| --- | --- | --- |
| 治理状态一致性 DEV-02/03/05 | 内部实现已有；真实发布回归与审计检索仍待验收 | 不重复实现；继续真实注册/治理/发布流程验证 |
| 样例 DEV-04 | 大小护栏与受保护清理已有 | 二进制对象策略与生产留存仍未闭合 |
| 可观测性 OBS-TP06/10/13/14 | MCP矩阵、真实状态、实时流和整体治理仍有退出条件 | 复用已完成 TP01~05/07~09/11/12；管理心跳、调用事实UI及正文清理切片已有；继续业务心跳、全局快照与整体治理 |
| 可观测性 OBS-TP15/16 | 全链路与平台交付尚未闭合 | 依赖相关功能及当前版本 PostgreSQL/Linux/多进程/性能证据 |
| 安全 SEC-C3/C4/E1 | 稳定文件源及 Gateway 配置激活已验证 | 管理Reload/意图结果审计已验证；已新增MCP可信映射与显式单跳共享Resolver；已补归属/发布/最新profile单SELECT读取与装配；upstream及后续验证/激活非整体一致事务；Watch、多进程、一致仓储读取及托管启动链接入、运行中撤销与自动逐跳/DNS网络边界仍待完成 |
| 安全 SEC-D2 | 主链先鉴权再缓存，缓存身份保护继续完善 | 与可观测性并行；Connection提名及代理生成头边界已补；完整限流层次和D1业务allowlist尚未闭合 |
| 外部与发布 | EXT-01~09、ENV-01、SEC-OPS-01 尚有环境验收；依赖审计需当前证据 | 受控夹具不替代真实环境；OAuth2 的 EXT-10 按 SEC-G1 延期处理 |

关闭验收项需记录目标环境、版本、执行命令、结果与脱敏证据；不得用本地组件通过代替跨平台或发布验收。2026-09-08 的本地 PostgreSQL/SQLite 43 表验证为历史有效证据，不代表当前加入可观测性后的数据库模型仍为43表，也不替代当前整合版本全链路验证。
## 1. 外部环境验收

状态：受环境可用性限制阻塞，不是代码基线阻塞。

下列当前范围内测试项需在发布前验收，不应视为已完成；标注延期的 EXT-10 属独立里程碑：

- `EXT-01`：真实上游 OpenAPI 导入，使用绝对服务器 URL
- `EXT-02`：未解析的 OpenAPI 导入，通过绑定实际运行时实例修复
- `EXT-03`：针对真实上游的手工 API 注册
- `EXT-04`：将某 API 从下线主机/端口 A 迁移到在线主机/端口 B，无需重新导入
- `EXT-05`：通过配置前缀执行实时 Gateway 聚合服务消费者探测
- `EXT-06`：使用选定传输与运行时凭据执行实时 MCP 消费者探测
- `EXT-07`：失败候选回归时保留上一次已知可用的 Gateway 与 MCP 版本
- `EXT-08`：Windows 平台 API/UI 交互启动与基础导入/转换流程
- `EXT-09`：完整 Ubuntu 安装、构建、启动、解析器校验与 Streamable 会话路径
- `EXT-10`（随 SEC-G1 延期，不属于当前 JWT/API Key/显式 Anonymous 里程碑）：真实 OAuth 提供方/JWKS 轮换、HTTPS 反代及第三方 MCP 客户端授权链路（资源元数据、audience、scope、重连）。本地签名令牌测试不能替代该验收项。
- `ENV-01`：完整 Windows `/health` 磁盘探测受当前限制环境权限影响（WMI/用户目录权限报错）而受阻，不应通过放宽系统执行策略规避。多进程本地联调只使用 `/api/health/ready`，不能当作全量系统探测通过的证据。

受控进展（2026-09-06）：`npm run verify:runtime-security-integration` 已在真实 API/MCP 进程、HTTPS 反代、HTTPS JWKS 轮换、SDK 调用、受保护的管理侧边清单与跨进程 API 证据场景下通过。该结果缩小了风险面，但尚未关闭 `EXT-10`：issuer 登录/授权同意及真实发布配置仍需外部验收。

执行细节与证据字段定义见 `docs/testing/runtime-publication-acceptance-cases.md`。

OAuth2 EXT-10 恢复后需提供：受信任 issuer/JWKS URL、Gateway/MCP 公网 URL 与反向代理配置、测试凭据本地路径。完成真实登录/同意流程、客户端刷新与重连后，再次执行 `npm run verify:runtime-closure`。本地 43 表 PG/SQLite 门禁已通过；安全夹具仍直接种子化路由快照，不覆盖从注册、治理、发布到托管部署的端到端流程。

## 2. 设计与实现偏差

主架构保持对齐，但以下产品化缺口仍未关闭。

### AUDIT-01：调用分析与检索工作流

状态：限定 HTTP 契约已验证，剩余 UI/实时与全链路收敛归 OBS-TP13/15。

调用明细、分侧正文、trace、调用者/来源、统计、事件历史和投递管理已经实现，不能继续列为未开发。28/28 HTTP 接口为限定契约 VERIFIED，API27/28覆盖新事件与新正文留存，AVAILABLE=0。权限、签名分页与脱敏以当前 API 契约为准；完整 UI/受控导出不能由接口验证推定完成。

### AUDIT-02：审计留存与存储生命周期

状态：OBS-TP14 继续实施；生产部署要求由 SEC-OPS-01 验收。

已具备正文对象存储与过期状态、组件级回收基础及投递查询。新事件/正文策略、策略管理UI与默认关闭的有界正文GC已验证；已补GC同扫描容量样本与只读诊断UI；完整策略、配额强制、其余元数据清理及备份恢复仍未闭合。本轮投递记录30天留存与事件可重投资格分离的结果见专项执行台账；不得将局部到期字段等同实际保留和磁盘治理保证。

### AUDIT-03：持久性与高并发保障

状态：持久 Store、增量采集检查点、事务事件/Outbox、投递租约与有限重试已实现；整体故障与承载矩阵仍待 OBS-TP14/16。

采集已有有界预算及写入失败指标，不能继续写成完全没有队列护栏或恢复。源文件写失败仍可能 fail-open，不保证零丢失。慢盘/满盘、强制终止、多进程、大正文和持续负载需按当前整合版本验证；不能通过重放业务调用补审计记录。

### AUDIT-04：调用方清单规模化与多主机采集

状态：调用者/来源增量持久投影与授权查询已完成；多主机生产采集与容量验证仍未闭合。

当前接口不再逐次扫描全部原始日志；复用 Store、Collector 和 CallersProjector，不新建第二索引链路。不增加调用方预注册要求，不将观测身份变为授权授予。整体来源健康与多主机能力归 OBS-TP10/14/16。

当前录入、查询、事件和投递合同见[可观测性 API 契约](./runtime-observability-api-endpoints.md)；安全范围见[安全执行台账](../guides/security-development-execution-status.md)。
### DEV-01：MCP 发布端点配置

优先级：P1

后端发布 DTO 已支持 MCP 的 `port` 与 `transport`，但受管发布/运行时 UI 尚未暴露它们。显式且可校验的 MCP `endpointPath` 合同也尚未落地。因此运营方目前不能在启用前完整定义 MCP 发布端点。

### DEV-02：上游变更校验状态

优先级：P1

状态：内部实现完成；真实发布回归仍待外部验收。

修改运行时上游绑定会修改修订号，并立即将关联运行时资产标记为 `verificationRequired=true`，记录原因、时间和上下文。部署/重部署阶段仍需执行验证；成功激活后清除标记。

### DEV-03：实例感知治理证据

优先级：P1

状态：内部实现完成；真实发布回归仍待外部验收。

关联测试实例归档、地址/端口/凭据等实例变更、默认实例切换和探测结果变化，都会显式失效关联运行时资产的验证标记。端点合约修订仍由发布候选 revision 负责；预发布摘要现在可直接读取 `verificationRequired` 状态。

### DEV-04：样例存储与留存控制

优先级：P1

状态：第一阶段内部实现完成；二进制专用对象存储和生产留存策略仍属后续项。

成功样例会自动保存并做脱敏；超过体量上限时保存摘要元数据而不是完整载荷。归档样例可通过受保护清理入口按保留天数删除。当前仅覆盖 JSON/文本摘要，二进制专用对象存储仍需外部部署决策。

### DEV-05：实例与绑定变更审计

优先级：P2

状态：内部实现完成；操作者身份透传与运营审计检索仍待后续工作。

源实例和上游绑定的创建、更新、归档、默认切换、探测、删除/替换均写入现有 `audit_logs`，资源类型、操作、before/after、原因和关联 revision 可用于后续检索。审计写入失败不阻断已完成的配置变更，生产告警/持久队列仍由 `AUDIT-02`/`AUDIT-03` 负责。

## 3. 运行时资产可观测性加固

状态：进行中。

- 扩展运行时资产优先模型下系统日志、审计日志与指标透视
- 深化“发布-运行时-监控”链路关联
- `/v1/servers/*` 仍承担当前受管进程管理与观测职责；运行时资产视图整合不应引入旧事件或旧持久化格式适配

## 4. 前端结构与打包整理

状态：待发布行为稳定后再执行。

- 在真实验收路径稳定后拆分 `EndpointRegistry`
- 保留注册、治理、发布三者职责分离
- 用测量数据处理生产环境分块警告，完成必要的代码分割

## 5. 国际化与编码加固

状态：持续维护。

- 替换相关区域中的可见乱码
- 将剩余固定文案收口到 locale 模块
- 避免非必要的视觉与内容大改动

## 6. 安全与通知投递

状态：通知投递仍待处理，以下发布门禁与后续能力各自带有独立状态。

- 邮件校验码投递
- 重置密码邮件投递
- 其他邮件通知投递
- 更广泛安全流程完整性复盘

### SEC-DEP-01：生产依赖安全公告检查

状态：当前依赖审计待执行，归安全 TP-F3a；历史阻塞不等同当前漏洞结果。

2026-09-06 曾因网络/审批受阻，2026-09-07 已有历史依赖告警快照（见安全执行台账）。本轮未重新联网审计，不能把旧失败写成从无证据，也不能把历史数量当作当前漏洞数。发布前需重审当前锁文件、生产可达性和兼容补丁，不自动 audit fix 或重大升级。

### SEC-OPS-01：生产安全与审计发布验收

状态：生产发布前强制项，依赖目标部署环境。

- 确认当前 JWT/API Key/显式 Anonymous 策略、适用的 issuer/audience/scope、TLS/反代路径及 Host/Origin 白名单。当前禁用 OAuth Discovery，资源元数据流程随 SEC-G1 延期。
- 复核已持久化的 Gateway 快照，变更认证策略须重新验证并发布；管理端 JWT、上游凭据与外部调用者令牌须分离。不得把历史 OAuth 路由或快照视为当前范围已验收。
- 建立持久绝对审计目录、Windows NTFS ACL 或 Linux 所有权/权限、加密磁盘与备份、运行时配额/留存护栏；当前文件模式未建立 Windows ACL，且无法自动识别任意业务密钥。
- 将 `[RUNTIME_AUDIT_WRITE_FAILED]` 与运维告警闭环，并明确受理 fail-open/丢失行为。执行访问拒绝、告警触达、备份/恢复演练，避免仅用单元故障注入代替部署验收。
- 发布前确认应用与迁移 CLI 共用的数据库环境、目标空库及备份/恢复流程，再按当前实体与初始化基线创建结构。不支持历史结构原地升级；当前本地验证未修改现有业务数据库。

仅凭脱敏后的环境级证据和记录到位的运营决策方可关闭；留存与持久化补强仍分别在 `AUDIT-02`/`AUDIT-03` 跟踪。

### SEC-AUTH-01：额外令牌适配与撤销能力

状态：能力设计延后，不属于已实施的 JWT 资源服务器合同。

不透明令牌内省、即时撤销与通用多 Issuer 适配器未实现。请先确认真实提供商要求，再扩展适配；需定义内省鉴权、缓存/异常时行为与撤销会话处理。当前 RS256/ES256 JWT 校验采用信任 issuer+JWKS 与过期控制。ApiNova 仍为资源服务器，不构建授权服务器，也不自动信任任意 issuer。

### SEC-POLICY-01：跨协议精细鉴权与 QoS

状态：设计/审批延后，不在当前日志与鉴权范围内。

当前 Gateway/MCP 已有 JWT/API Key 的限定 scope 与工具过滤；统一路由/API 策略、跨协议限流、QoS 分级/配额/优先级仍未闭合。现有 Gateway 的流控不能被表述为跨协议 QoS。实施前需定义 scope 继承、准入结果和可观测拒绝记录，同时保持 MCP HTTP/SSE/Streamable 语义与 ApiNova 轻量网关边界。

## 证据边界

已完成行为与测试计数记录于上述设计/验收文档，不应在此文件作为未完成项处理。历史 7/38/40 表结果不代表当前模型；2026-09-08 历史43表 PG/SQLite 本地验证与受控安全联调也不关闭外部身份提供商、托管发布、UI/平台及生产运维验收。
