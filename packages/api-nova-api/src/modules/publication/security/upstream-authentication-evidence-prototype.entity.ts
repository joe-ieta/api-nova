// Backwards-compatible name for the isolated challenge service. Storage registration
// does not register its service, create an HTTP entrypoint, or authorize publication.
export { UpstreamAuthenticationEvidenceEntity as UpstreamAuthenticationEvidencePrototype } from '../../../database/entities/upstream-authentication-evidence.entity';
